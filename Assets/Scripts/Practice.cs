/*

using UnityEngine;

public class Practice : MonoBehaviour
{
    void Start()
    {
        float d = Vector2.Distance(Vector2.up, Vector2.down * 2);

        float num = 77.7f;

        float abs = Mathf.Abs(num);
        float pow = Mathf.Pow(num, 2.5f);

        float n = Absolute(-45);
    }

    float Absolute(float number)
    {
        if (number >= 0)
            return number;
        else
            return -number;
    }
}

*/